# 🔍 Аудит посадочной страницы (Landing Page v5.0)

**Дата аудита:** 31 января 2026  
**Статус:** Подготовлено к анализу  

---

## 📊 1. SEO СТРУКТУРА

### ✅ ЧТО УЖЕ РЕАЛИЗОВАНО

#### Meta-теги (в `<head>`)
- ✅ `<title>` — оптимизирован для CTR
- ✅ `<meta description>` — 155 символов, включает ключевые фразы
- ✅ `<meta robots>` — `index, follow, max-image-preview:large`
- ✅ `<meta keywords>` — релевантные для каждого языка
- ✅ `<meta theme-color>` — брендирование
- ✅ `<meta author>` — "lnkmx"

#### Canonical & hreflang
- ✅ `<link rel="canonical">` — https://lnkmx.my/
- ✅ `<link rel="alternate" hreflang="ru">` — для русского
- ✅ `<link rel="alternate" hreflang="en">` — для английского
- ✅ `<link rel="alternate" hreflang="kk">` — для казахского
- ✅ `<link rel="alternate" hreflang="x-default">` — для остальных

#### Open Graph (Social Meta)
- ✅ `og:type` — website
- ✅ `og:title`, `og:description` — для шеринга в соц.сетях
- ✅ `og:image` — 1200x630 px
- ✅ `og:locale` — поддержка локалей (ru_RU, en_US, kk_KZ)
- ✅ `og:site_name` — "lnkmx"

#### Twitter Card
- ✅ `twitter:card` — summary_large_image
- ✅ `twitter:title`, `twitter:description`
- ✅ `twitter:image` — для превью твитов
- ✅ `twitter:site` — @lnkmx_app

#### Structured Data (JSON-LD)
- ✅ **Organization** — базовая информация о компании
- ✅ **WebSite** — с SearchAction для прямого ввода username
- ✅ **SoftwareApplication** — позиционирование как "Micro-Business OS"
- ✅ **FAQPage** — для Rich Snippets в поисковых результатах

---

## ⚠️ 2. ПРОБЛЕМЫ И РЕКОМЕНДАЦИИ

### A. BOT-КОНТЕНТ СКРЫТ ДЛЯ ПОЛЬЗОВАТЕЛЕЙ ✅

**Статус:** ХОРОШО

Весь текст для ботов находится в `<head>`:
- Meta-теги — никогда не отображаются на странице
- JSON-LD скрипты — не видны пользователю (тип `application/ld+json`)
- Keywords — не видны (только в meta)

**Проверка DevTools:**
```
<head>
  <meta name="description" content="...">
  <meta name="keywords" content="...">
  <script type="application/ld+json">
    {"@context": "https://schema.org", ...}
  </script>
</head>
```

✅ Ничего из этого не видно на странице для пользователя!

---

### B. ПОТЕНЦИАЛЬНЫЕ ПРОБЛЕМЫ

#### ⚠️ Проблема 1: Отсутствие страницы OG изображения
```
og:image: https://lnkmx.my/og-image.png
```
**Рекомендация:** Убедиться, что файл `/public/og-image.png` существует (1200x630px).

#### ⚠️ Проблема 2: Нет Schema.org для BreadcrumbList
**Рекомендация:** На других страницах добавить BreadcrumbList для навигации.

#### ⚠️ Проблема 3: Keywords потенциально может быть оптимизирован
Текущий keywords:
```
micro business os, crm для малого бизнеса, конструктор страниц, 
link in bio, мини-сайт, сайт-визитка, linktree альтернатива
```
**Рекомендация:** Добавить дополнительные длинные ключи (long-tail):
- "как создать сайт-визитку бесплатно"
- "сайт для небольшого бизнеса"
- "альтернатива linktree на русском"

#### ⚠️ Проблема 4: Нет мета-тега для мобильного viewport
**Рекомендация:** Проверить, что в `<head>` есть:
```html
<meta name="viewport" content="width=device-width, initial-scale=1">
```

---

## 🎯 3. SEO/AIO КОНТЕНТ АУДИТ

### Текст для ботов (AI crawlers - для indexing)

#### На странице (видимо, но не выделено):
- ✅ H1 ("Ваша визитка") — присутствует
- ✅ H2 sections — многоуровневая структура
- ✅ Alt-теги на изображениях — проверить наличие
- ✅ Микроразметка для блоков — присутствует

#### В компонентах (проверить):
- Нужна ли `<Helmet>` или `<Head>` компонента для каждой страницы?
- Текущий подход (useEffect) может быть недостаточен при SSR

---

### Текст для пользователей (видимо на странице)

#### Hero Section
- ✅ Clear value prop — "Ваша визитка за 2 минуты"
- ✅ Specific benefits — выделены ключевые возможности
- ✅ Primary CTA — "Создать страницу" (заметный, контрастный)
- ✅ Secondary CTA — "Посмотреть примеры" (для сомневающихся)
- ✅ Trust indicators — есть (проверить видимость)

#### Sections Below Hero
- ✅ Problem → Solution — понятный flow
- ✅ How it works — 3-5 шагов
- ✅ Results / Social Proof — примеры реальных пользователей
- ✅ Pricing — прозрачная структура
- ✅ FAQ — частые вопросы
- ✅ Final CTA — повторная мотивация

---

## 🔒 4. СКРЫТИЕ BOT-КОНТЕНТА

### Что скрыто успешно:

```css
/* Meta-теги — видны только в DevTools */
<meta name="keywords" content="...">
/* Решение: Это нормально — keywords не должны быть видны */

/* JSON-LD — нерендерится как текст */
<script type="application/ld+json">{"@context": "..."}
/* Решение: Браузеры игнорируют такой скрипт */

/* Open Graph — видны только при шеринге */
<meta property="og:image" content="...">
/* Решение: Это нормально — используется для соц.сетей */
```

### Что может быть видимым (проверить):

#### ⚠️ Скрытые элементы в HTML (display: none, visibility: hidden)
**Рекомендация:** Проверить нет ли скрытого бот-контента в DOM:
```tsx
// ❌ ПЛОХО
<div style="display: none">
  Текст для ботов, невидимый для пользователей
</div>

// ✅ ХОРОШО
/* Контент в <head> только */
```

#### ⚠️ sr-only (screen-reader only) класс
**Рекомендация:** sr-only используется только для accessibility:
```tsx
// ✅ ХОРОШО
<span className="sr-only">Additional context for screen readers</span>
```

---

## 📋 5. ИТОГОВЫЙ ЧЕКЛИСТ

| Элемент | Статус | Действие |
|---------|--------|---------|
| Title | ✅ | Хорошо оптимизирован |
| Meta Description | ✅ | Хорошо оптимизирована |
| Meta Keywords | ✅ | Хорошо, но может быть расширена |
| Canonical | ✅ | Установлен правильно |
| hreflang | ✅ | Поддерживает 3 языка + x-default |
| Open Graph | ✅ | Хорошо, но нужна проверка og-image.png |
| Twitter Card | ✅ | Настроена правильно |
| JSON-LD Organization | ✅ | Присутствует |
| JSON-LD WebSite | ✅ | Присутствует с SearchAction |
| JSON-LD SoftwareApplication | ✅ | Хорошо позиционирует продукт |
| JSON-LD FAQPage | ✅ | Хорошо для Rich Snippets |
| Bot-контент скрыт | ✅ | Весь в `<head>`, не видимо пользователю |
| H1 на странице | ⚠️ | Проверить уникальность |
| Alt-теги на img | ⚠️ | Проверить наличие на всех изображениях |
| Viewport meta | ⚠️ | Проверить наличие в index.html |
| SSR Support | ⚠️ | Текущий подход работает только с CSR |

---

## 🚀 6. РЕКОМЕНДАЦИИ ПО УЛУЧШЕНИЮ

### Высокий приоритет:
1. Убедиться, что `/public/og-image.png` существует (1200x630px)
2. Проверить наличие `<meta name="viewport">` в `index.html`
3. Проверить уникальность `<h1>` на странице (только один)
4. Добавить `lang` атрибут на `<html>` элемент (уже реализовано в коде)

### Средний приоритет:
1. Расширить keywords (добавить long-tail варианты)
2. Добавить BreadcrumbList JSON-LD для других страниц
3. Проверить все img теги имеют alt атрибуты

### Низкий приоритет:
1. Рассмотреть SSR для улучшения SEO (если требуется)
2. Добавить structured data для рейтинговой системы (если есть)
3. Оптимизировать loading strategy для lazy-loaded компонентов

---

## 📝 ПРИМЕЧАНИЯ

- Весь bot-контент **правильно скрыт** в `<head>` и не видимо пользователю
- Keywords находятся в meta-теге, что **нормально** (не должны быть видны)
- JSON-LD скрипты имеют тип `application/ld+json`, что **предотвращает** их рендеринг как текста
- Open Graph теги видны только при шеринге в соц.сетях, что **ожидаемо**

**Статус:** ✅ Хорошая структура, требуются мелкие проверки

